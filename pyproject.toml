[project]
name = "pandas-tut"
version = "0.1.0"
description = "Comprehensive Pandas tutorial covering DataFrames, Series, CRUD operations, data cleaning, and advanced filtering with practical examples."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.3.3",
]
